# reflexivity

Repositorio para el proyecto "reflexivity".

## Instalación

1. Crea un entorno virtual:

```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
```

2. Instala dependencias:

```bash
pip install -r requirements.txt
```

## Uso

- Añade tus scripts en la raíz o en `utils/`.
- Los datos se encuentran en `data/`.

## Contribuir

Haz forks, commits y pull requests al repositorio principal.

---

_Puedes editar este README para añadir información específica del proyecto._
